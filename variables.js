const names = ["ab", "bee", "c"];

// for (index in names) {
// console.log(names.filter((name) => name.length === 5));
// console.log(names.map((name) => name.length === 3));
// }

try {
  throw "yaskd";
} catch (err) {
  console.log("err");
}
